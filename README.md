## Running Tests

This setup assumes you have python >=3.11 installed.

Run:
```sh
python -m venv interview_venv
source interview_venv/bin/activate
pip install -r requirements.txt
pytest test_logger.py
```
